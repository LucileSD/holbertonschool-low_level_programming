#include "main.h"
/**
*mul - multiplies two integers
*@a: represent the first number
*@b: represent the second number
*
*Return: 0
*/
int mul(int a, int b)
{
	int result;

	result = a * b;

	if (a > 0)
	{
		return (result);
	}
	else
	{
		return (result);
	}
	return (0);
}
